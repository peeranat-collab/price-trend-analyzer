import streamlit as st
import pandas as pd

st.set_page_config(page_title="วิเคราะห์แนวโน้มราคา", layout="wide")

st.title("📊 เว็บวิเคราะห์แนวโน้มราคา")

st.write("ระบบนี้ใช้สำหรับวิเคราะห์แนวโน้มราคาสินค้า เช่น น้ำมัน เหล็ก ฯลฯ")

st.header("ตัวอย่างกราฟ")

data = pd.DataFrame({
    "วัน": ["จันทร์", "อังคาร", "พุธ", "พฤหัส", "ศุกร์"],
    "ราคา": [100, 105, 102, 110, 108]
})

st.line_chart(data.set_index("วัน"))
